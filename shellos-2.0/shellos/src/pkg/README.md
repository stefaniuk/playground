This directory contains package installation scripts.

Package installation:
---------------------

    pkg/${package}/install
        pkg/${package}/details
        pkg/${package}/install-${DIST}-${DEVICE}
            pkg/${package}/compile-${DIST}-${DEVICE}
            pkg/${package}/config-${DIST}-${DEVICE}

