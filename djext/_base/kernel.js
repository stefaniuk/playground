define([
], function() {

    // module:
    //        djext/_base/kernel
    // summary:
    //        This module is the foundational module of the djext boot sequence; it defines the djext object.

    // create djext
    var djext = {
        topic: {},
        baseUrl: '/'
    };

    return djext;
});
