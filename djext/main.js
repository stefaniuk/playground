define([
    './_base/kernel',
    './_base/ready',
    './_base/registry',
    './_base/html'
], function(kernel, ready, registry, html) {

    // module:
    //        djext/main
    // summary:
    //        This is the package main module for the djext package; it loads djext base.

    return kernel;
});
